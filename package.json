{
  "name": "serializer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "webpack ./src/*.js ./www/bundle.js --watch source-map",
    "dev": "tsc -w",
    "serve": "lite-server",
    "rollup": "rollup --format=iife --output=./www/app.js -- src/app.js",
    "watch":"npm run npm-watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "liteserver": "^0.3.0",
    "npm-watch": "^0.1.8",
    "typescript": "^2.2.2"
  },
  "dependencies": {
    "core-decorators": "^0.15.0",
    "typedjson-npm": "^0.1.6"
  }
}
